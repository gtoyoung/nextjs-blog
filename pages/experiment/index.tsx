import Experiment from "components/experiment/exp";
import { Layout } from "components/layout";
import React from "react";

const ExperimentPage = () => {
  return (
    <Layout>
      <Experiment />
    </Layout>
  );
};

export default ExperimentPage;
